schema_version = "1.4"
artifacts = ["/app/pipeline.py"]

[task]
name = "rajbhadani/task-05-currency-parsing"
version = "1.0.0"
description = "Fix currency-string numeric parsing."

[metadata]
category = "data-pipeline"
difficulty = "medium"

[agent]
timeout_sec = 300

[verifier]
timeout_sec = 120
environment_mode = "separate"

[verifier.environment]
os = "linux"
network_mode = "no-network"

[environment]
os = "linux"
network_mode = "no-network"