# Task 1: Sales Join Mismatch

The pipeline combines sales records with product reference data and writes a monthly revenue report.

The pipeline runs successfully, but the reported revenue totals are incorrect for some products.

Investigate the input data and pipeline logic. Fix the bug in the pipeline without modifying the input data files.

Run the pipeline and ensure the output is saved to:

`/app/output/monthly_revenue.csv`
