schema_version = "1.4"

artifacts = ["/app/pipeline.py"]

[task]
name = "rajbhadani/task-01-sales-join"
version = "1.0.0"
description = "Fix an incorrect sales-to-product data join."

[metadata]
category = "data-pipeline"
difficulty = "medium"

[agent]
timeout_sec = 300

[verifier]
timeout_sec = 120
environment_mode = "separate"

[verifier.environment]
os = "linux"
network_mode = "no-network"
cpus = 1
memory_mb = 512

[environment]
os = "linux"
network_mode = "no-network"
cpus = 1
memory_mb = 1024
